import React from 'react';
import PropTypes from 'prop-types';

const kontakt = (props) => {
    return (
        <div>
            <h1>Kontakt</h1>
        </div>
    );
};

kontakt.propTypes = {};

export default kontakt;

import * as React from 'react';

const IndexPage = () => {
    return (
        <main style={{ height: '150vh' }}>
            <p
                style={{
                    height: '300px',
                    background: 'black',
                    marginBottom: '50px',
                }}
            ></p>
        </main>
    );
};

export default IndexPage;
